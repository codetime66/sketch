import java.util.*;

class Main {
    public static void main(String[] args) {
        System.out.println(Locale.GERMAN.getDisplayLanguage(Locale.FRENCH));
            // allemand

        System.out.println(Locale.GERMAN.getDisplayLanguage(Locale.CHINA));
            // Deutsch
    }
}